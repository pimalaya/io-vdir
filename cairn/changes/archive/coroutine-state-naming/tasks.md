---
cairn: tasks
change: coroutine-state-naming
---

- [x] Rename the create, write and rename waits to `CreateDir`, `CreateFile`, `Rename`, `Remove`, `RemoveFiles`
- [x] Rename the read waits to `Read`, `ReadDir`, `ReadFiles`, `ReadChildren`, `ReadMetadata`, `ReadRandom`
- [x] Rename the existence waits to `Probe`, `ProbeDirs`, `ProbeMetadata`
- [x] Reword every `Display` phrase to a present verb and its object, naming what each step acts on
- [x] Keep `Start` and `Locate` as they are, both already present tense
